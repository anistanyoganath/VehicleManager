// lib/core/theme/app_colors.dart

import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors - Automotive Inspired
  static const primary = Color(0xFF2563EB); // Trustworthy blue
  static const primaryLight = Color(0xFF3B82F6);
  static const primaryDark = Color(0xFF1D4ED8);

  // Secondary/Accent Colors
  static const accent = Color(0xFF10B981); // Success green for maintenance
  static const warning = Color(0xFFF59E0B); // Warning orange for reminders
  static const danger = Color(0xFFEF4444); // Red for expiry

  // Neutral Colors
  static const background = Color(0xFFF8FAFC);
  static const surface = Colors.white;
  static const cardBackground = Colors.white;

  // Text Colors
  static const textPrimary = Color(0xFF1E293B);
  static const textSecondary = Color(0xFF64748B);
  static const textHint = Color(0xFF94A3B8);

  // Status Colors
  static const urgent = Color(0xFFDC2626);
  static const upcoming = Color(0xFFF59E0B);
  static const completed = Color(0xFF10B981);
}
